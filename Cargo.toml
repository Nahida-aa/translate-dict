[workspace]
members = ["crates/translate-dict-lsp"]

[workspace.package]
version = "0.1.0"

[package]
name = "translate-dict"
version.workspace = true
edition = "2021"
publish = false

[lib]
name = "translate_dict"
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7.0"

[profile.dist]
inherits = "release"
lto = "thin"

[package.metadata.dist]
dist = false
